================================================================================
 62 — PRE-COMMIT HYGIENE + SECRET-LEAK AUDIT  (v1.24.0 release candidate)
================================================================================
Scope    : entire uncommitted change set — tracked mods + untracked new files +
           both submodule working trees (LLMsVerifier, claude-code-router).
Mode     : READ-ONLY. The only file written is this report.
Date     : 2026-07-20
Host repo: /home/milos/Factory/projects/tools_and_research/claude_toolkit

--------------------------------------------------------------------------------
 VERDICT:  SAFE-TO-COMMIT  (hygiene + secret-leak scope — no hard blockers)
           with 2 RELEASE-READINESS ADVISORIES that the operator MUST act on
           before tagging v1.24.0 (submodule pointers are NOT actually bumped;
           the new submodule code is uncommitted). Details in section A.
--------------------------------------------------------------------------------

Every finding below carries file:line + the command that produced it. No real
secret is printed; the three "key-shaped" hits are all synthetic test/doc
fixtures and are quoted only because they are demonstrably NOT real credentials.

================================================================================
 CHANGE SET ENUMERATED  (git status --porcelain ; per-submodule git status)
================================================================================
MAIN REPO — modified tracked (46):
  AGENTS.md CHANGELOG.md CLAUDE.md Claude_Multi_Account_Fine_Tuning.md GEMINI.md
  QWEN.md docs/Provider_Aliases_User_Guide.md docs/Provider_FAQ.md
  docs/Provider_Verification_Guide.md scripts/model_verify.py
  scripts/providers/overrides.json scripts/providers_resolve.py
  scripts/tests/lib/sandbox.sh scripts/tests/run-all.sh scripts/tests/run-proof.sh
  scripts/tests/test_install.sh test_kimi.sh test_output_tokens.sh
  test_providers.sh test_session_flags.sh test_sessions.sh test_wrapper_exec.sh
  verify_helixagent_test.sh verify_providers_live.sh
  scripts/verify_superpowers_tui.sh + 20 scripts/tests/proof/* evidence files
MAIN REPO — untracked new (12 files):
  docs/research/antibluff_20260720/BRIEF.md
  docs/research/credit_detection_20260720/BRIEF.md
  docs/research/gateway_20260720/BRIEF.md
  scripts/providers/credit-endpoints.json
  scripts/tests/lib/suite-lock.sh
  scripts/tests/test_ccr_conformance.sh  test_mutation_residue.sh
  scripts/tests/test_provider_credit.sh  test_providers_gate.sh
  scripts/tests/test_sandbox_hygiene.sh  test_sandbox_leak.sh  test_suite_lock.sh
SUBMODULE LLMsVerifier (dirty at f9b875cf):
  M README.md ; M llm-verifier/pkg/i18n/bundles/active.en.yaml
  ?? docs/CREDIT_AWARE_SELECTION.md providers/credit_signal.go(+_test)
     scoring/selection_adapter.go(+_test) llm-verifier/selection/
SUBMODULE claude-code-router (detached HEAD at 5a7a5e7):
  M cmd/ccr/main.go ; M cmd/ccr/service.go
  ?? cmd/ccr/launch.go launch_exec_test.go launch_test.go
     main_testmain_test.go restart_replay_test.go restart_test.go

================================================================================
 A. RELEASE-READINESS ADVISORIES  (act before tagging — not a file-hygiene fault)
================================================================================
A1. [MUST-RESOLVE] Submodule pointers are NOT actually bumped; new code is
    uncommitted inside the submodules.
    Evidence:
      git submodule status:
        f9b875cf... submodules/LLMsVerifier      (llmsverifier-v1.12.2-1-gf9b875cf)
        5a7a5e7...  submodules/claude-code-router (v0.4.9)
      git ls-tree HEAD (committed pointer):
        f9b875cf... submodules/LLMsVerifier
        5a7a5e7...  submodules/claude-code-router
    The committed pointer == current submodule HEAD for BOTH. The "…-dirty"
    seen in `git diff -- submodules/*` is ONLY the dirty-marker; committing the
    superproject now records NO pointer change. The v1.24.0 submodule code
    (credit_signal.go, selection_adapter.go, selection/, ccr launch.go, the
    main.go/service.go edits) is UNCOMMITTED. Committing only the superproject
    would ship a release whose pointers reference commits that LACK the new
    code — i.e. an incomplete v1.24.0.
    Also: LLMsVerifier `main` is "ahead 1" of origin/main (that HEAD is unpushed
    and does not contain the new untracked files); claude-code-router is on a
    DETACHED HEAD (no branch). Operator must: commit inside each submodule →
    put ccr on a branch → push both → then bump the superproject pointers.

A2. [MINOR] `.superpowers/` exclusion is not durable via the tracked .gitignore.
    It IS excluded from this commit — `git status --ignored` shows `!! .superpowers/`
    and it never appears as `??`. The rule is `.superpowers/sdd/.gitignore:1:*`
    (an untracked, self-ignoring nested .gitignore). The tracked top-level
    .gitignore has NO `.superpowers/` rule (`grep -nE superpower .gitignore` → none).
    Safe for THIS commit; recommend adding `.superpowers/` to the tracked
    .gitignore so scratch content can never leak on another machine.

A3. [ADVISORY] One oversized file WILL be committed:
      2,197,652 bytes  scripts/tests/proof/40-sandbox-suite.log  (~2.1 MB, modified)
    It is an intentionally-tracked proof/evidence artifact, but it is large.
    (The 24 MB / 23 MB / 2.2 MB / 1.4 MB binaries under submodules/LLMsVerifier
     are ALREADY-TRACKED pre-existing files — not part of this change set — and
     ccr bin/ccr (32 MB) is gitignored. See section C.)

================================================================================
 1. SECRET LEAKS  — RESULT: CLEAN (no real credential material)
================================================================================
Scanner (the toolkit's own boundary-anchored regex, test_lib.sh:121/142):
  RE='(^|[^A-Za-z0-9_-])(sk-ant-|sk-|gsk_|xai-|hf_|AIza|xoxb-|xoxp-|xoxs-|pc-|
       re_|secret_|ghp_|github_pat_|AKIA)[A-Za-z0-9_-]{12,}'
Coverage: added lines of the tracked diff (main), full content of every
untracked new file (main), full content of every modified proof/evidence file,
tracked diff added lines + untracked files of BOTH submodules, plus a broader
supplementary pass (Bearer/Authorization/x-api-key headers, password|token|
secret|api_key assignments ≥16 chars, hex ≥40 / base64 ≥60 blobs).

Total key-shaped hits: 3 — every one a synthetic fixture (REDACTED here):
  (i)   scripts/tests/test_provider_credit.sh:659
          secret = "sk-live-[REDACTED]"      <- fabricated value fed to
          mv.redact()/probe_paid_model() to ASSERT the key is replaced by
          "[REDACTED]" and never survives into a cached record (lines 659-669).
  (ii)  scripts/tests/test_provider_credit.sh:669
          "sk-[REDACTED]" not in mv.redact("token sk-[REDACTED] here")
          <- fabricated value proving the generic scrubber catches shapes it
          was never handed. Both (i)/(ii) are in a NEW test file; both are the
          "fixture asserting it IS flagged/redacted" case — not real secrets.
  (iii) submodules/claude-code-router/cmd/ccr/launch_exec_test.go:139
          t.Setenv("ANTHROPIC_API_KEY", "sk-ant-[REDACTED-PLACEHOLDER]")
          <- literal spells out "REAL-USER-KEY-MUST-NOT-BE-FORWARDED"; it is the
          fixture for a SECURITY guard test (TestLaunchDropsAnthropicAPIKey)
          proving the key is NOT forwarded to the gateway. Not a real key.

Supplementary broad-pass noise (all benign, NOT secrets):
  - scripts/tests/proof/40-sandbox-suite.log — many lines matched the hex-≥40
    heuristic; every one is a Go fuzz-corpus filename in "rm: cannot remove
    …/go/pkg/mod/…/testdata/fuzz/…: Permission denied" cleanup output. Not keys.
  - docs/research/innovations/05-data-models-and-schemas.md:1670
      export CMA_FAKE_SECRET="sk-should-never-print-[REDACTED]"
    This file is ALREADY TRACKED and UNMODIFIED (out of this change set; not
    being re-committed). The value is a documentation example of an anti-leak
    test pattern ("should-never-print"). Benign + out of scope.

Full-content scan of the 20 modified proof/evidence files (live-provider logs
are the likeliest accidental-leak surface): ZERO key-shaped hits, ZERO
Authorization/x-api-key headers.
  cmd: git status --porcelain | grep '^ M' | …proof/|evidence… | grep -E "$RE"  -> empty
  cmd: …grep -niE 'authorization: bearer …|x-api-key: …'                        -> empty

Conclusion: NO real secret in the change set. Not a release blocker.

================================================================================
 2. STRAY / UNINTENDED FILES  — RESULT: CLEAN
================================================================================
grep over main + both submodule status for editor backups / residue:
  \.(orig|bak|rej|mut|pyc|class|o|core)$ | ~$ | _mutated_ | .DS_Store |
  node_modules | __pycache__ | .superpowers | .swp | core.[0-9]+
  -> NO MATCHES.
Untracked new files are all intended (3 research briefs, 1 credit-endpoints.json,
8 test/*.sh). Sizes 4K-52K each (du -h) — nothing oversized among new files.
Tracked-but-gitignored check:
  git ls-files -i -c --exclude-standard   -> EMPTY  (nothing tracked that
  .gitignore says to ignore).
Oversized (>1 MB) files touched by this change set: only 40-sandbox-suite.log
(2.1 MB) is in-scope and committable — see A3.

================================================================================
 3. MUTATION RESIDUE in shippable code  — RESULT: CLEAN (empty)
================================================================================
cmd: grep -rnE 'MUTATION|&& false|// always (pass|return)|_mutated_' \
       scripts submodules/LLMsVerifier/llm-verifier \
       submodules/claude-code-router/cmd \
       --include='*.sh' --include='*.py' --include='*.go' \
     | grep -vE 'test_mutation_residue\.sh|_test\.go'
result: (clean: no mutation residue in shippable code)
  — the only files legitimately containing those patterns (the mutation-residue
    scanner and *_test.go security fixtures) are excluded, as specified.

================================================================================
 4. PARSE / COMPILE INTEGRITY  — RESULT: ALL PASS
================================================================================
python3 -m py_compile:
  OK  scripts/model_verify.py
  OK  scripts/providers_resolve.py
bash -n  (all 21 changed/new .sh in the main repo):
  OK  scripts/tests/lib/sandbox.sh          OK  scripts/tests/lib/suite-lock.sh
  OK  scripts/tests/run-all.sh              OK  scripts/tests/run-proof.sh
  OK  scripts/tests/test_ccr_conformance.sh OK  scripts/tests/test_install.sh
  OK  scripts/tests/test_kimi.sh            OK  scripts/tests/test_mutation_residue.sh
  OK  scripts/tests/test_output_tokens.sh   OK  scripts/tests/test_provider_credit.sh
  OK  scripts/tests/test_providers_gate.sh  OK  scripts/tests/test_providers.sh
  OK  scripts/tests/test_sandbox_hygiene.sh OK  scripts/tests/test_sandbox_leak.sh
  OK  scripts/tests/test_session_flags.sh   OK  scripts/tests/test_sessions.sh
  OK  scripts/tests/test_suite_lock.sh      OK  scripts/tests/test_wrapper_exec.sh
  OK  scripts/tests/verify_helixagent_test.sh  OK verify_providers_live.sh
  OK  scripts/verify_superpowers_tui.sh
JSON validity (python3 json.load):
  OK  scripts/providers/overrides.json
  OK  scripts/providers/credit-endpoints.json
  OK  scripts/tests/proof/providers-summary.json
go build ./...  (go1.26.4):
  OK  submodules/claude-code-router                 (go vet ./cmd/ccr/ clean)
  OK  submodules/LLMsVerifier/llm-verifier
  No stray build binaries emitted into either working tree (go build ./... for
  main packages discards output; the pre-existing untracked llm-verifier/selection/
  dir was present BEFORE the build, not created by it).

================================================================================
 5. .gitignore CORRECTNESS  — RESULT: CORRECT (1 minor advisory, A2)
================================================================================
- docs/research/ (3 new BRIEF.md: antibluff/credit_detection/gateway) — INTENDED
  to be committed as research briefs. Correct. Scanned clean; 4K-52K each.
- .superpowers/ — excluded from commit (see A2); currently safe, but the rule
  lives in an untracked nested .gitignore rather than the tracked one.
- No ambiguous or contradictory ignore state; nothing tracked that should be
  ignored (section 2).

================================================================================
 6. GOVERNANCE-DOC LOCKSTEP  — RESULT: PASS (spread 0.91%, well within ±5%)
================================================================================
Sizes (stat -c %s):
  CLAUDE.md  18705 bytes  (171 lines)   dev -0.68%
  AGENTS.md  18876 bytes  (172 lines)   dev +0.23%
  QWEN.md    18876 bytes  (172 lines)   dev +0.23%
  GEMINI.md  18876 bytes  (172 lines)   dev +0.23%
  mean 18833 bytes ; min-max spread = 0.91% of mean  (limit ±5%)  -> PASS
Content sanity: diff CLAUDE.md AGENTS.md -> exactly ONE differing line, the
AGENTS.md lockstep-notice header comment ("maintained in lockstep … §11.4.157").
Bodies are otherwise byte-identical. Four docs are in genuine lockstep.
(The authoritative size check was run directly rather than `run-all.sh
constitution` to avoid contending for the suite lock held by another stream,
per the task constraint.)

================================================================================
 SUMMARY
================================================================================
 1 Secret leaks ............... CLEAN  (3 hits, all synthetic fixtures; live
                                        logs clean of keys + auth headers)
 2 Stray/unintended files ..... CLEAN  (1 large tracked proof log — A3)
 3 Mutation residue ........... CLEAN  (empty)
 4 Parse/compile integrity .... PASS   (py + 21 sh + 3 json + 2 go builds)
 5 .gitignore correctness ..... CORRECT (minor: .superpowers not in tracked
                                         ignore — A2)
 6 Governance-doc lockstep .... PASS   (0.91% spread)

 HARD BLOCKERS: none in the hygiene/secret scope -> SAFE-TO-COMMIT.
 BEFORE TAGGING v1.24.0, resolve A1 (submodules are dirty at the SAME committed
 SHA — the pointer bumps do NOT exist yet and the new submodule code is
 uncommitted/unpushed; a superproject-only commit ships an incomplete release).
================================================================================
